services:
  - type: web
    name: nova-wa-gateway
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: API_KEY
        generateValue: true
      - key: NODE_VERSION
        value: 20
